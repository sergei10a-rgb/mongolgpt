{
  "$schema": "https://v2-8-13.turborepo.dev/schema.json",
  "globalEnv": ["CI", "MONGOLGPT_DISABLE_SHARE", "MONGOLGPT_EXPERIMENTAL_DISABLE_FILEWATCHER"],
  "globalPassThroughEnv": ["CI", "MONGOLGPT_DISABLE_SHARE", "MONGOLGPT_EXPERIMENTAL_DISABLE_FILEWATCHER"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "mongolgpt#test": {
      "dependsOn": ["^build"],
      "outputs": [],
      "passThroughEnv": ["*"]
    },
    "@mongolgpt/core#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@mongolgpt/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@mongolgpt/ui#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@mongolgpt/session-ui#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
