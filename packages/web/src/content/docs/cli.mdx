---
title: CLI лавлагаа
description: MongolGPT CLI-ийн команд, сонголт, орчны хувьсагчийн лавлагаа.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI-г [GitHub Releases](https://github.com/sergei10a-rgb/mongolgpt/releases)-ээс суулгасны дараа хувилбараа шалгана.

```bash
mongolgpt --version
```

## Түргэн эхлэх

MongolGPT CLI-г аргументгүй ажиллуулахад [TUI](../tui) нээгдэнэ.

```bash
mongolgpt
```

Нэг удаагийн ажлыг интерактив интерфэйсгүй ажиллуулах бол `run` командыг ашиглана.

```bash
mongolgpt run "Энэ төслийн унаж байгаа тестийг олж тайлбарла."
```

Provider холбох, model сонгох үндсэн дараалал:

```txt
/connect
/models
```

Командын тусламжийг аль ч түвшинд `--help` сонголтоор харж болно.

```bash
mongolgpt --help
mongolgpt run --help
```

---

### tui

MongolGPT терминалын хэрэглэгчийн интерфэйсийг эхлүүлнэ үү.

```bash
mongolgpt [project]
```

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Сүүлийн сессийг үргэлжлүүлэх |
| <nobr><code>{"--session"}</code></nobr> | `-s` | Үргэлжлүүлэхийн тулд сесс ID |
| <nobr><code>{"--fork"}</code></nobr> |       | `--continue` эсвэл `--session`-ээр нээх сессийг fork хийнэ |
| <nobr><code>{"--prompt"}</code></nobr> |       | Эхлэхэд илгээх prompt |
| <nobr><code>{"--model"}</code></nobr> | `-m` | `provider/model` хэлбэрийн model ID |
| <nobr><code>{"--agent"}</code></nobr> |       | Ашиглах агент |
| <nobr><code>{"--port"}</code></nobr> |       | Сонсох порт |
| <nobr><code>{"--hostname"}</code></nobr> |       | Сонсох хостын нэр |
| <nobr><code>{"--mdns"}</code></nobr> |       | mDNS илрүүлэлтийг идэвхжүүлэх |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Захиалгат mDNS домэйн нэр |
| <nobr><code>{"--cors"}</code></nobr> |       | CORS-д зөвшөөрөх browser origin; олон удаа өгч болно |

---

## Командууд

MongolGPT CLI нь мөн дараах тушаалуудтай.

---

### agent

MongolGPT-н агентуудыг удирдах.

```bash
mongolgpt agent [command]
```

---

#### create

Өөрчлөн тохируулсан шинэ агент үүсгэнэ үү.

```bash
mongolgpt agent create
```

Энэ команд нь тусгай системийн хүсэлт, зөвшөөрлийн тохиргоо бүхий шинэ агент үүсгэхэд тань туслах болно. Таны зөвшөөрөөгүй бүх зүйлийг үүсгэсэн агентын нүүрэнд үгүйсгэдэг.

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr> |       | Агент файлыг бичих лавлах (өгөгдмөл нь глобал юмуу `.mongolgpt/agent` сануулгыг үндэслэн) |
| <nobr><code>{"--description"}</code></nobr> |       | Агент юу хийх ёстой вэ |
| <nobr><code>{"--mode"}</code></nobr> |       | Агент горим: `all`, `primary`, эсвэл `subagent` |
| <nobr><code>{"--permissions"}</code></nobr> |       | Зөвшөөрөх зөвшөөрлийн таслалаар тусгаарлагдсан жагсаалт (өгөгдмөл: бүгд). Боломжтой: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Орхигдсон бүх зүйлийг үгүйсгэдэг. Нэр: `--tools` |
| <nobr><code>{"--model"}</code></nobr> | `-m` | Ашиглах загвар, `provider/model` формат |

`--path`, `--description`, `--mode`, `--permissions`-г бүгдийг нь дамжуулснаар командыг интерактив бус байдлаар ажиллуулна.

---

#### list

Боломжтой бүх агентуудыг жагсаана уу.

```bash
mongolgpt agent list
```

---

### attach

`serve` эсвэл `web` тушаалуудаар эхлүүлсэн аль хэдийн ажиллаж байгаа MongolGPT backend серверт терминалыг холбоно уу.

```bash
mongolgpt attach [url]
```

Энэ нь алсын удирдлагатай TUI-г ашиглах боломжийг олгодог. Жишээ нь:

```bash
# Вэб болон мобайл хандалтын серверийг эхлүүлнэ
mongolgpt web --port 4096 --hostname 0.0.0.0

# Өөр терминалаас TUI-г ажиллаж буй серверт холбоно
mongolgpt attach http://10.20.30.40:4096
```

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr> |       | TUI-г \|-д эхлүүлэх ажлын лавлах |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Сүүлийн сессийг үргэлжлүүлэх |
| <nobr><code>{"--session"}</code></nobr> | `-s` | Үргэлжлүүлэхийн тулд сесс ID |
| <nobr><code>{"--fork"}</code></nobr> |       | Үргэлжлүүлэхдээ сессийг хойшлуул (`--continue` эсвэл `--session` ашиглана уу) |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Баталгаажуулах үндсэн нууц үг (өгөгдмөл нь `MONGOLGPT_SERVER_PASSWORD`) |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Баталгаажуулах үндсэн хэрэглэгчийн нэр (өгөгдмөл нь `MONGOLGPT_SERVER_USERNAME` эсвэл `mongolgpt`) |

---

### auth

Итгэмжлэх жуух бичгийг удирдах, үйлчилгээ үзүүлэгчдийн нэвтрэх тушаал.

```bash
mongolgpt auth [command]
```

---

#### login

MongolGPT нь [Models.dev](https://models.dev) хаягаар үйлчилгээ үзүүлэгчийн жагсаалтаар тэжээгддэг тул та ашиглахыг хүссэн бүх үйлчилгээ үзүүлэгчдээ API товчлуурыг тохируулахын тулд `mongolgpt auth login` ашиглаж болно. Үүнийг `~/.local/share/mongolgpt/auth.json`-д хадгална.

```bash
mongolgpt auth login
```

MongolGPT эхлэхэд итгэмжлэлийн файлаас үйлчилгээ үзүүлэгчдийг ачаална. Хэрэв таны орчинд тодорхойлогдсон түлхүүрүүд эсвэл таны төсөлд `.env` файл байгаа бол.

##### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p` | Үйлчилгээ үзүүлэгч ID эсвэл \| руу нэвтрэх нэр |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Ашиглах нэвтрэх аргын шошго, аргын сонголтыг алгасах |

---

#### list

Итгэмжлэлийн файлд хадгалагдсан бүх баталгаажсан үйлчилгээ үзүүлэгчдийг жагсаана.

```bash
mongolgpt auth list
```

Эсвэл богино хувилбар.

```bash
mongolgpt auth ls
```

---

#### logout

Таныг итгэмжлэлийн файлаас устгаснаар үйлчилгээ үзүүлэгчээс гарна.

```bash
mongolgpt auth logout
```

---

### github

Хадгалах газрын автоматжуулалтын GitHub агентийг удирдана уу.

```bash
mongolgpt github [command]
```

---

#### install

GitHub агентийг өөрийн сандаа суулгана уу.

```bash
mongolgpt github install
```

Энэ нь шаардлагатай GitHub Үйлдлийн ажлын урсгалыг тохируулж, тохиргооны үйл явцыг удирдан чиглүүлдэг. [Илүү ихийг мэдэж аваарай](../github).

---

#### run

GitHub агентийг ажиллуул. Үүнийг ихэвчлэн GitHub Үйлдлүүдэд ашигладаг.

```bash
mongolgpt github run
```

##### Сонголтууд

| Туг | Тодорхойлолт |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub төлөө агентыг ажиллуулах хуурамч үйл явдал |
| <nobr><code>{"--token"}</code></nobr> | GitHub хувийн хандалтын токен |

---

### mcp

Загварын контекст протоколын серверүүдийг удирдах.

```bash
mongolgpt mcp [command]
```

---

#### add

Өөрийн тохиргоонд MCP сервер нэмнэ үү.

```bash
mongolgpt mcp add
```

Энэ тушаал нь таныг дотоод эсвэл алсын MCP сервер нэмэхэд чиглүүлэх болно.

---

#### list

Бүх тохируулсан MCP серверүүд болон тэдгээрийн холболтын статусыг жагсаа.

```bash
mongolgpt mcp list
```

Эсвэл богино хувилбарыг ашиглана уу.

```bash
mongolgpt mcp ls
```

---

#### auth

OAuth идэвхжүүлсэн MCP серверээр баталгаажуулна уу.

```bash
mongolgpt mcp auth [name]
```

Хэрэв та серверийн нэр өгөхгүй бол OAuth боломжтой серверүүдээс сонгохыг хүсэх болно.

Та мөн OAuth чадвартай серверүүд болон тэдгээрийн баталгаажуулалтын статусыг жагсааж болно.

```bash
mongolgpt mcp auth list
```

Эсвэл богино хувилбарыг ашиглана уу.

```bash
mongolgpt mcp auth ls
```

---

#### logout

MCP серверийн OAuth итгэмжлэлийг устгана уу.

```bash
mongolgpt mcp logout [name]
```

---

#### debug

MCP серверийн OAuth холболтын асуудлыг дибаг хийх.

```bash
mongolgpt mcp debug <name>
```

---

### models

Тохируулсан үйлчилгээ үзүүлэгчээс боломжтой бүх загваруудыг жагсаана уу.

```bash
mongolgpt models [provider]
```

Энэ тушаал нь `provider/model` форматаар таны тохируулсан үйлчилгээ үзүүлэгчд байгаа бүх загваруудыг харуулдаг.

Энэ нь [таны тохиргоо](../config/)-д яг ямар загварын нэрийг ашиглахыг олоход хэрэгтэй.

Та сонголтоороо ID үйлчилгээ үзүүлэгчээр дамжуулж загваруудыг тухайн үйлчилгээ үзүүлэгчээр шүүж болно.

```bash
mongolgpt models anthropic
```

#### Сонголтууд

| Туг | Тодорхойлолт |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | models.dev \| сайтаас загварын кэшийг сэргээнэ үү |
| <nobr><code>{"--verbose"}</code></nobr> | Илүү дэлгэрэнгүй загвар гаралтыг ашиглах (зардал гэх мэт мета өгөгдөл орно) |

`--refresh` тугийг ашиглан кэштэй загварын жагсаалтыг шинэчилнэ үү. Энэ нь үйлчилгээ үзүүлэгчид шинэ загвар нэмэгдсэн бөгөөд та тэдгээрийг MongolGPT дээр харахыг хүсвэл хэрэг болно.

```bash
mongolgpt models --refresh
```

---

### run

Сануулгыг шууд дамжуулж интерактив бус горимд mongolgpt-г ажиллуулна уу.

```bash
mongolgpt run [message..]
```

Энэ нь скрипт бичих, автоматжуулалт эсвэл TUI-г бүрэн эхлүүлэхгүйгээр хурдан хариулт авахыг хүссэн үед хэрэгтэй. Жишээ нь.

```bash "mongolgpt run"
mongolgpt run Go хэлэнд context хэрхэн ашигладгийг тайлбарла
```

Та мөн ажиллаж байгаа `mongolgpt serve` жишээнд хавсаргаж болох бөгөөд гүйлт болгонд MCP серверийг ачаалахаас зайлсхийх боломжтой.

```bash
# Нэг терминалд headless серверийг эхлүүлнэ
mongolgpt serve

# Өөр терминалаас серверт холбогдох команд ажиллуулна
mongolgpt run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ------------------------------------------------------------ | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr> |       | Ажиллуулах тушаал, args \| мессежийг ашиглана уу |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Сүүлийн сессийг үргэлжлүүлэх |
| <nobr><code>{"--session"}</code></nobr> | `-s` | Үргэлжлүүлэхийн тулд сесс ID |
| <nobr><code>{"--fork"}</code></nobr> |       | Үргэлжлүүлэхдээ сессийг хойшлуул (`--continue` эсвэл `--session` ашиглана уу) |
| <nobr><code>{"--share"}</code></nobr> |       | Сессийг хуваалцах |
| <nobr><code>{"--model"}</code></nobr> | `-m` | Үйлчилгээ үзүүлэгч/загвар хэлбэрээр ашиглах загвар |
| <nobr><code>{"--agent"}</code></nobr> |       | Ашиглах агент |
| <nobr><code>{"--file"}</code></nobr> | `-f` | Мессежид хавсаргах файл(ууд) |
| <nobr><code>{"--format"}</code></nobr> |       | Формат: анхдагч (форматлагдсан) эсвэл json (түүхий JSON үйл явдлууд) |
| <nobr><code>{"--title"}</code></nobr> |       | Сеансын гарчиг (утга заагаагүй бол тайрсан сануулгыг ашигладаг) |
| <nobr><code>{"--attach"}</code></nobr> |       | Ажиллаж байгаа mongolgpt серверт хавсаргах (жишээ нь: http://localhost:4096) |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Баталгаажуулах үндсэн нууц үг (өгөгдмөл нь `MONGOLGPT_SERVER_PASSWORD`) |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Баталгаажуулах үндсэн хэрэглэгчийн нэр (өгөгдмөл нь `MONGOLGPT_SERVER_USERNAME` эсвэл `mongolgpt`) |
| <nobr><code>{"--dir"}</code></nobr> |       | Ажиллуулах лавлах эсвэл \| хавсаргах үед алсын сервер дээрх зам |
| <nobr><code>{"--port"}</code></nobr> |       | Локал серверийн порт (анхдагчаар санамсаргүй порт) |
| <nobr><code>{"--variant"}</code></nobr> |       | Загварын хувилбар (үйлүүлэгчийн тусгай үндэслэлийн хүчин чармайлт) |
| <nobr><code>{"--thinking"}</code></nobr> |       | Сэтгэлгээний блокуудыг харуулах |
| <nobr><code>{"--dangerously-skip-permissions"}</code></nobr> |       | Тодорхой үгүйсгээгүй зөвшөөрлийг автоматаар батлах (аюултай!) |

---

### serve

API хандахын тулд толгойгүй MongolGPT сервер ажиллуулна уу. Бүрэн HTTP интерфэйсийг [серверийн баримтууд](../server)-г үзнэ үү.

```bash
mongolgpt serve
```

Энэ нь TUI интерфэйсгүйгээр mongolgpt функцэд API хандалтыг хангадаг HTTP серверийг эхлүүлнэ. HTTP үндсэн баталгаажуулалтыг идэвхжүүлэхийн тулд `MONGOLGPT_SERVER_PASSWORD`-г тохируулна уу (хэрэглэгчийн нэр нь `mongolgpt`).

#### Сонголтууд

| Туг | Тодорхойлолт |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr> | Сонсох порт |
| <nobr><code>{"--hostname"}</code></nobr> | Сонсох хостын нэр |
| <nobr><code>{"--mdns"}</code></nobr> | mDNS илрүүлэлтийг идэвхжүүлэх |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Захиалгат mDNS домэйн нэр |
| <nobr><code>{"--cors"}</code></nobr> | CORS \| зөвшөөрөх нэмэлт хөтчийн гарал үүсэл(үүд). |

---

### session

MongolGPT сессүүдийг удирдах.

```bash
mongolgpt session [command]
```

---

#### list

Бүх MongolGPT сешнүүдийг жагсаа.

```bash
mongolgpt session list
```

##### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | Хамгийн сүүлийн үеийн N сессийг хязгаарлах |
| <nobr><code>{"--format"}</code></nobr> |       | Гаралтын формат: хүснэгт эсвэл json (хүснэгт) |

---

#### delete

MongolGPT сессийг устгах.

```bash
mongolgpt session delete <sessionID>
```

---

### stats

MongolGPT сессийн токен ашиглалт болон зардлын статистикийг харуул.

```bash
mongolgpt stats
```

#### Сонголтууд

| Туг | Тодорхойлолт |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr> | Сүүлийн N өдрийн статистикийг харуулах (бүх цаг үед) |
| <nobr><code>{"--tools"}</code></nobr> | Харуулах хэрэгслийн тоо (бүгд) |
| <nobr><code>{"--models"}</code></nobr> | Загварын ашиглалтын задаргааг харуулах (өгөгдмөлөөр нуугдсан). Шилдэг N \|-г харуулахын тулд дугаарыг дамжуулаарай |
| <nobr><code>{"--project"}</code></nobr> | Төслөөр шүүх (бүх төсөл, хоосон мөр: одоогийн төсөл) |

---

### export

Сеанс өгөгдлийг JSON хэлбэрээр экспортлох.

```bash
mongolgpt export [sessionID]
```

Хэрэв та ID сессийг өгөхгүй бол боломжтой сешнүүдээс сонгохыг танаас хүсэх болно.

#### Сонголтууд

| Туг | Тодорхойлолт |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Эмзэг хуулбар/файлын өгөгдлийг засварлах |

---

### import

JSON файлаас сессийн өгөгдөл импортлоно.

```bash
mongolgpt import <file>
```

Одоогийн launch profile-д public share backend дэмжигдэхгүй тул локал export файлыг ашиглана.

```bash
mongolgpt import session.json
```

---

### web

Вэб интерфэйстэй толгойгүй MongolGPT серверийг эхлүүл.

```bash
mongolgpt web
```

Энэ нь HTTP серверийг эхлүүлж, вэб интерфэйсээр дамжуулан MongolGPT руу хандах вэб хөтчийг нээнэ. HTTP үндсэн баталгаажуулалтыг идэвхжүүлэхийн тулд `MONGOLGPT_SERVER_PASSWORD`-г тохируулна уу (хэрэглэгчийн нэр нь `mongolgpt`).

#### Сонголтууд

| Туг | Тодорхойлолт |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr> | Сонсох порт |
| <nobr><code>{"--hostname"}</code></nobr> | Сонсох хостын нэр |
| <nobr><code>{"--mdns"}</code></nobr> | mDNS илрүүлэлтийг идэвхжүүлэх |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Захиалгат mDNS домэйн нэр |
| <nobr><code>{"--cors"}</code></nobr> | CORS \| зөвшөөрөх нэмэлт хөтчийн гарал үүсэл(үүд). |

---

### acp

ACP (Agent Client Protocol) серверийг эхлүүлнэ үү.

```bash
mongolgpt acp
```

Энэ тушаал нь nd-JSON ашиглан stdin/stdout-ээр холбогддог ACP серверийг эхлүүлнэ.

#### Сонголтууд

| Туг | Тодорхойлолт |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr> | Ажлын лавлах |
| <nobr><code>{"--port"}</code></nobr> | Сонсох порт |
| <nobr><code>{"--hostname"}</code></nobr> | Сонсох хостын нэр |
| <nobr><code>{"--mdns"}</code></nobr> | mDNS илрүүлэлтийг идэвхжүүлэх |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Захиалгат mDNS домэйн нэр |
| <nobr><code>{"--cors"}</code></nobr> | CORS \| зөвшөөрөх нэмэлт хөтчийн гарал үүсэл(үүд). |

---

### plugin

Plugin суулгаад тохиргоогоо шинэчил.

```bash
mongolgpt plugin <module>
```

Эсвэл өөр нэрийг ашиглана уу.

```bash
mongolgpt plug <module>
```

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g` | Глобал тохиргоонд суулгана уу |
| <nobr><code>{"--force"}</code></nobr> | `-f` | Одоо байгаа залгаасын хувилбарыг солих |

---

### pr

GitHub PR салбарыг дуудаж, тооцоо хийгээд дараа нь MongolGPT ажиллуулна уу.

```bash
mongolgpt pr <number>
```

---

### db

Өгөгдлийн сангийн хэрэгслүүд.

```bash
mongolgpt db [query]
```

#### Сонголтууд

| Туг | Тодорхойлолт |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Гаралтын формат: `json` эсвэл `tsv` |

---

#### path

Өгөгдлийн сангийн замыг хэвлэх.

```bash
mongolgpt db path
```

---

### debug

Дибаг хийх, алдааг олж засварлах хэрэгсэл.

```bash
mongolgpt debug [command]
```

---

### uninstall

MongolGPT-г устгаад холбогдох бүх файлыг устгана уу.

```bash
mongolgpt uninstall
```

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Тохиргооны файлуудыг хадгалах |
| <nobr><code>{"--keep-data"}</code></nobr> | `-d` | Сеансын өгөгдөл болон агшин зуурын зургийг хадгалах |
| <nobr><code>{"--dry-run"}</code></nobr> |       | Устгахгүйгээр юу устгахыг харуулах |
| <nobr><code>{"--force"}</code></nobr> | `-f` | Баталгаажуулах сануулгыг алгасах |

---

### upgrade

mongolgpt-г хамгийн сүүлийн хувилбар эсвэл тодорхой хувилбар руу шинэчилнэ.

```bash
mongolgpt upgrade [target]
```

Хамгийн сүүлийн хувилбар руу шинэчлэхийн тулд.

```bash
mongolgpt upgrade
```

Тодорхой хувилбар руу шинэчлэхийн тулд.

```bash
mongolgpt upgrade v0.1.48
```

#### Сонголтууд

| Туг | Богино | Тодорхойлолт |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Суулгах арга: `curl`, `npm`, `pnpm`, `bun`, `brew` |

---

## Глобал сонголтууд

MongolGPT CLI нь дараах глобал сонголтуудыг хүлээн авна.

| Туг | Богино | Тодорхойлолт |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr> | `-h` | Тусламжийг харуулах |
| <nobr><code>{"--version"}</code></nobr> | `-v` | Хувилбарын дугаарыг хэвлэх |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Log-ийг `stderr` рүү хэвлэх |
| <nobr><code>{"--log-level"}</code></nobr> |       | Бүртгэлийн түвшин (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr> |       | Гадаад залгаасгүйгээр ажиллуулах |

---

## Орчны хувьсагчид

MongolGPT орчны хувьсагчийг ашиглан тохируулж болно.

| Хувьсагч | Төрөл | Тодорхойлолт |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `MONGOLGPT_AUTO_SHARE` | логикийн | Public share backend дэмжигдэхгүй тул launch-д бүү идэвхжүүл |
| `MONGOLGPT_GIT_BASH_PATH` | мөр | Windows дээр ашиглах Git Bash executable-ийн зам |
| `MONGOLGPT_CONFIG` | мөр | Тохиргооны файлын зам |
| `MONGOLGPT_TUI_CONFIG` | мөр | TUI тохиргооны файл руу орох зам |
| `MONGOLGPT_CONFIG_DIR` | мөр | Тохируулгын лавлах зам |
| `MONGOLGPT_CONFIG_CONTENT` | мөр | Inline JSON тохиргоо |
| `MONGOLGPT_DISABLE_AUTOUPDATE` | логикийн | Автомат шинэчлэлтийн шалгалтыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_PRUNE` | логикийн | Хуучин өгөгдлийг тайрахыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_TERMINAL_TITLE` | логикийн | Автомат терминалын гарчгийн шинэчлэлтийг идэвхгүй болгох |
| `MONGOLGPT_PERMISSION` | мөр | Доторлогдсон json зөвшөөрлийн тохиргоо |
| `MONGOLGPT_DISABLE_DEFAULT_PLUGINS` | логикийн | Өгөгдмөл залгаасуудыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_LSP_DOWNLOAD` | логикийн | LSP серверийн автомат таталтыг идэвхгүй болгох |
| `MONGOLGPT_ENABLE_EXPERIMENTAL_MODELS` | логикийн | Туршилтын загваруудыг идэвхжүүлэх |
| `MONGOLGPT_DISABLE_AUTOCOMPACT` | логикийн | Автомат контекст нягтруулахыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_CLAUDE_CODE` | логикийн | `.claude` prompt болон skill дэмжлэгийг бүхэлд нь унтраах |
| `MONGOLGPT_DISABLE_CLAUDE_CODE_PROMPT` | логикийн | `~/.claude/CLAUDE.md` уншихыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_CLAUDE_CODE_SKILLS` | логикийн | `.claude/skills` ачаалахыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_MODELS_FETCH` | логикийн | Алсын эх сурвалжаас загвар татахыг идэвхгүй болгох |
| `MONGOLGPT_DISABLE_MOUSE` | логикийн | TUI mouse interaction-ийг идэвхгүй болгох |
| `MONGOLGPT_FAKE_VCS` | мөр | Туршилтын зорилгоор хуурамч VCS үйлчилгээ үзүүлэгч |
| `MONGOLGPT_CLIENT` | мөр | Үйлчлүүлэгчийн танигч (өгөгдмөл нь `cli`) |
| `MONGOLGPT_ENABLE_EXA` | логикийн | Exa web search tool-ийг идэвхжүүлэх |
| `MONGOLGPT_SERVER_PASSWORD` | мөр | `serve` болон `web`-ийн HTTP Basic authentication-ийг идэвхжүүлэх |
| `MONGOLGPT_SERVER_USERNAME` | мөр | Basic authentication хэрэглэгчийн нэр; өгөгдмөл нь `mongolgpt` |
| `MONGOLGPT_MODELS_URL` | мөр | Загварын тохиргоог дуудах захиалгат URL |

---

### Туршилтын хувьсагчид

Эдгээр орчны хувьсагч нь өөрчлөгдөж эсвэл устгаж болох туршилтын функцуудыг идэвхжүүлдэг.

| Хувьсагч | Төрөл | Тодорхойлолт |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `MONGOLGPT_EXPERIMENTAL` | логикийн | Туршилтын боломжуудын ерөнхий flag-ийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_ICON_DISCOVERY` | логикийн | Дүрс илрүүлэхийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT` | логикийн | TUI-д сонгоход автоматаар хуулахыг идэвхгүй болгох |
| `MONGOLGPT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | тоо | Bash command-ийн өгөгдмөл timeout, millisecond-оор |
| `MONGOLGPT_EXPERIMENTAL_OUTPUT_TOKEN_MAX` | тоо | LLM хариултын гаралтын token-ийн дээд хэмжээ |
| `MONGOLGPT_EXPERIMENTAL_FILEWATCHER` | логикийн | Туршилтын file watcher-ийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_OXFMT` | логикийн | `oxfmt` formatter-ийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_LSP_TOOL` | логикийн | Туршилтын LSP tool-ийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_DISABLE_FILEWATCHER` | логикийн | Файл ажиглагчийг идэвхгүй болгох |
| `MONGOLGPT_EXPERIMENTAL_EXA` | логикийн | Туршилтын Exa функцуудыг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_LSP_TY` | логикийн | Python файлд `ty` LSP-г идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_PLAN_MODE` | логикийн | Төлөвлөгөөний горимыг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_BACKGROUND_SUBAGENTS` | логикийн | Арын дэд агентийн ажлыг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_EVENT_SYSTEM` | логикийн | Туршилтын үйл явдлын системийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_NATIVE_LLM` | логикийн | Native LLM request path-ийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_PARALLEL` | логикийн | Зэрэгцээ вэб хайлтын гүйцэтгэлийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_SCOUT` | логикийн | Scout subagent-ийг идэвхжүүлэх |
| `MONGOLGPT_EXPERIMENTAL_WORKSPACES` | логикийн | Ажлын талбарын дэмжлэгийг идэвхжүүлэх |

